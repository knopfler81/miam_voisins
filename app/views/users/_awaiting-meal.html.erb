  <div class="tab-content hidden" id="awaiting-meal">
    <% if @user.placed_orders.present? %>
      <% @user.placed_orders.coming.each do|meal| %>
        <div class="col-xs-12 col-md-3">
          <div class="card-meal">
          <%= link_to meal_path(meal) do  %>

            <div class="row">
              <div class="col-xs-12 text-center">
                <div class="col-xs-5">
                  <h3><%= meal.menu_name %></h3>
                  <% if  meal.images?%>
                    <%= cl_image_tag meal.images.first.path, :transformation=>[
                    {:width=>150, :height=>75, :crop=>"fill"}], class: "meal-image"%>
                 <% end %>
                  <h3> <%= humanized_money_with_symbol(meal.price) %></h3>
                </div>
                <div class="col-xs-7">
                 <h3><i class="fa fa-cutlery" aria-hidden="true"></i> <%= meal.user.nickname  %></h3>
                 <p><%= meal.availability.strftime("%d/%m/%Y")%></p>
                 <p><%= meal.location %></p>
                </div>
              </div>

             <div class="col-xs-12 text-center ">
               <h4><%= meal.category.name %></h4>
             </div>

            </div>
          </div>

          <% end %>
        </div>
      <% end %>
    <% end %>
  </div>
