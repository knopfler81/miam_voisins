<!-- Repas commandé par le user, en attente -->
  <div class="tab-content hidden" id="awaiting-meal-ordered">
    <% if @user.placed_orders.present? %>
        <div class="col-md-8 col-md-offset-2 ">
      <% @user.placed_orders.coming.each do|meal| %>
        <%= link_to meal_path(meal) do  %>

        <%= render "shared/meal_card", meal: meal %>

        <% end %>

      <% end %>
    <% end %>
  </div>

